
<h1><%= t('.header') %><h1/>

<p>
  <%= link_to t('.new_test'), new_admin_test_path, class: 'btn btn-outline-warning' %>
</p>

<div class="row">
  <div class="table table-sm">
    <table class="table">
      <thead>
        <tr>
          <th scope="col"><%= t('.title') %></th>
          <th scope="col"><%= t('.level') %></th>
          <th scope="col"><%= t('.questions') %></th>
          <th colspan="3"><%= t('.action') %></th>
        </tr>
      </thead>
        <%= render @tests %>
    </table>
  </div>
</div>

